{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "bash build.sh"
  },
  "deploy": {
    "startCommand": "cd backend && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8787}",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
